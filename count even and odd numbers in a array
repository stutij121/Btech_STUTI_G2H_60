#include <stdio.h>
intmain()
int a[10],i,n,even=0,odd=0;
printf("enter the number of elements :");
scanf("%d",& n);

for(i=0;i<n;i++)
{
    scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
    scanf("%d"&a[i]);
}
for(i=0;i<n;i++)
{
    if(a[i]%2==0)
    even++;
    else
    odd++;
}
printf("even="%d\n",even);
    printf("odd=%d",odd);
    return 0;

}
