#include <stdio.h>
int main()
{
    int arr[10],i,n,max;
    printf("enter the elements:");
    scanf("%d",&n);
        for(i=0;i<n;i++)
        {
        scanf("%d",& arr[i]);
        }
        max=arr[0];
        for(i=1;i<n;i++)
        {
        if(arr[i]>max)
        {
            max=arr[i];
        }
    }
    
    printf("largest element =%d",max);
    return 0;
    }